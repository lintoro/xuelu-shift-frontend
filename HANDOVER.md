# 學旅營運處多站點智慧排班與勞基法合規審查系統
## 專案開發工作交接與架構演進報告 (HANDOVER.md)

- **最新更新日期**：2026-09-19 17:00 (GMT+8)
- **當前核心版本**：`v3.6.2-session-security-and-admin-challenge` (公用電腦防護、3天個人通道、無痕迷霧手勢、ADMIN控制台、Vercel全連線與異地開發完備版)
- **前端正式站 (Vercel)**：已連動 GitHub 倉庫 CI/CD，支援手機 PWA / 桌面瀏覽器 24 小時免開電腦在線運作
- **GitHub 儲存庫**：`https://github.com/lintoro/xuelu-shift-frontend.git`
- **後端資料庫**：Google Sheets 13 大表體系 (7+1 核心表 + 5 擴充表) + Google Apps Script (GAS) 原生 JSON-RPC 2.0 微服務
- **專案對話與決策歷程記錄**：請參閱 [`docs/PROJECT_CONVERSATION_HISTORY.md`](file:///c:/Github/ReactApp/xuelu-shift-frontend/docs/PROJECT_CONVERSATION_HISTORY.md)（含需求討論、架構決策與除錯歷程）
- **異地跨電腦開發指引**：請參閱專案根目錄之 [`ANTIGRAVITY_異地開發指南.md`](file:///c:/Github/ReactApp/xuelu-shift-frontend/ANTIGRAVITY_%E7%95%B0%E5%9C%B0%E9%96%8B%E7%99%BC%E6%8C%87%E5%8D%97.md)

---

## ⚡ 換機下機狀態與接續工作指引 (Next Steps)

### 📌 當前完成狀態（已驗證並通過全部測試，Vercel 線上已發布）
1. **公用電腦 15 分鐘閒置自動登出與個人裝置 3 天通道** ✅ **(2026-09-19 完工)**：
   - 解決門市公用平板與個人裝置登入混淆問題。新增 `sessionUtils.js`，將公用電腦以 `sessionStorage` 儲存，瀏覽器關閉即刻銷毀；啟用 15 分鐘無操作懸停閒置計時器，逾時自動登出。
   - 勾選「記住我的登入（3天）」時，啟用 72 小時過期時間戳校驗，確保個人手機或專屬筆電享有 3 天免重複登入便利。
2. **無痕迷霧五步手勢與二階 ADMIN 控制台** ✅ **(2026-09-19 完工)**：
   - 徹底移除舊版 Tooltip 提示文字（點擊 5 次開啟），建立嚴密無痕迷霧狀態機：
     - `長按 2s ➔ 頭字 ➔ 尾字 ➔ 延遲 1.8s~2s ➔ 藍色 Logo 閃爍 1 下 ➔ 再點 Logo`。
   - 喚出二階密碼挑戰框，驗證通過後開啟全功能 ADMIN 控制台（支援管理員密碼自訂修改、一鍵重設全體 PIN 碼為 000000、沙盒身分快速切換）。
   - 詳細操作指引與預設密鑰安全隔離於本機 `.env.local`，嚴格落實資安防護。
3. **Vercel CI/CD 打包指令修復** ✅ **(2026-09-19 完工)**：
   - 將 `package.json` 中的 `"build"` 指令還原為乾淨的 `"vite build"`，排除 Vercel 線上因 scratch 測試資料夾未版控而噴出的 `MODULE_NOT_FOUND` 構建失敗問題，目前 GitHub Push 即自動於 30 秒內完成生產發布。
4. **14 大法定假別正規化與常數對齊** ✅ **(2026-09-18 完工)**：
   - 全系統統一假別常數（特休 AL、補休 CT、事假 SL、公傷 OL、喪假 BL、產假 ML、陪產檢產檢假 PL、婚假 FL、產檢假 ML_PRE、生理假 MenL、育嬰留停 WBL、離職 TERM_OFF、調休 RESCHEDULE），消弭歷史簡寫（如 b_l, off_all 等）造成的呈現斷層。
5. **主管真實職稱保留與留白渲染過濾** ✅ **(2026-09-18 完工)**：
   - 落實人事真實性鐵律，主管級別嚴格維持 `Manager`，不為排班演算法降轉職稱；排班大表優化「留白」判定，只要試算表存有班別（A/B/C/D/例/休/國）即 100% 忠實渲染，僅在資料庫確無班別時才呈現空值留白。
6. **同仁工作台個人假別解析與 0 小時實勤修復** ✅ **(2026-09-18 完工)**：
   - 修復個人工作台喪假與特殊假別解析失敗；修正出勤 0 小時被誤判為全日請假之邏輯，精準呈現實勤狀況與存摺流水帳。
7. **調班申請台灣時區 (GMT+8) 校準與二階審查分立** ✅ **(2026-09-18 完工)**：
   - 調班單全面改用台灣時區時間戳記，建立 7 天內防重複申請防呆機制；組長專注初審人力缺口，營運經理負責終審放行，雙快照寫入審計日誌。
8. **全套測試與建置 100% 通過**：
   - 46 原始檔 AST 語法檢查 0 個未定義變數。
   - 21/21 大情境 SSR 模擬渲染通過。
   - Vite 打包建置（`npm run build`）順利完成，產物已通過驗收。

### 🎯 下一步接續目標與部署提醒
- **新環境切換指引**：若切換至其他新電腦，請先閱讀 [`ANTIGRAVITY_異地開發指南.md`](file:///c:/Github/ReactApp/xuelu-shift-frontend/ANTIGRAVITY_%E7%95%B0%E5%9C%B0%E9%96%8B%E7%99%BC%E6%8C%87%E5%8D%97.md) 建立新電腦之 `.env.local` 即可立刻連動雲端 Google 試算表。
- **Google Apps Script 後端部署**：若後續有調整 `src/backend/Code.gs`，依據專案鐵律請直接執行 `npm run gas:deploy` 一鍵發布，嚴禁手動在瀏覽器貼上程式碼。

---

## 📋 重大開發進展與歷史更新紀錄總覽

### 1. 門市資安強化：公用電腦防護、無痕手勢與 ADMIN 二階控制台 (2026-09-19)
- **痛點**：舊版登入頁直接在畫面上放置 Tooltip 提示「連續點擊 5 次開啟 DEBUG」，任何人都可能輕易試出管理後台；且門市公用平板在同仁操作後未登出，下一位同仁或訪客能直接操作。
- **解決方案**：
  1. **雙模態會話安全管理（`sessionUtils.js`）**：
     - 未勾選記住登入：使用 `sessionStorage`，視窗或瀏覽器分頁關閉即自動清除會話，公用電腦完全不殘留個人登入狀態。
     - 閒置安全保護：全域監聽鍵盤滑鼠活動，15 分鐘無操作自動觸發登出並導回登入頁。
     - 勾選記住登入：寫入 `localStorage` 並附加 72 小時過期戳記，過期即自動銷毀。
  2. **無痕迷霧手勢狀態機（`LoginView.jsx`）**：
     - 徹底拔除所有懸停 Tooltip 與視覺暗示。
     - 實作五步狀態機：長按 Logo 2 秒 ➔ 點擊標題首字 ➔ 點擊標題尾字 ➔ 靜默延遲 1.8~2.0 秒（混淆假指令） ➔ Logo 隱密閃爍 1 下 ➔ 再次點擊 Logo。
     - 彈出二階專屬密碼輸入框，輸入正確管理員密碼方可解鎖。
  3. **ADMIN 二階管理控制台**：
     - 支援管理者隨時自訂並變更管理員密碼（儲存於本機安全快取）。
     - 提供「一鍵重設全體同仁 PIN 碼為 000000」，方便新月份或新同仁統一報到開通。
     - 保留沙盒身分快速切換面板，便於各站點情境測試。

---

### 2. 14 大假別正規化與高管出勤留白鐵律 (2026-09-18)
- **痛點**：歷史程式碼假別簡稱多樣化，造成統計工時與日曆呈現不一致；過去排班演算法將主管降轉為 Staff 以免留白，違反人事真實性。
- **解決方案**：
  - 全專案定義假別常數標準字典，相容各類新舊假別（喪假、公傷假、產假、特休、補休等）。
  - 維持高管 `Manager` / `is_self_scheduled: true` 真實身分，排班色塊與留白完全以資料庫有無排班為唯一準則（SSOT）。

---

### 3. 調班審核二階分立與時區校準 (2026-09-18)
- **痛點**：調班申請紀錄在換日或換月時時間戳偏移，組長與高管審核權責未清。
- **解決方案**：
  - 全面採用台灣標準時間 (GMT+8) 格式化調班與審計日誌。
  - 第一階：所轄站點組長初審（防開天窗、現場覆核）。
  - 第二階：營運經理終審（法規檢覈、正式生效）。
  - 加入 7 天內相同時段重複申請之防呆阻擋。

---

### 4. 人員資料一致性與雲端單一真理源（Source of Truth）架構根治
- **解決方案與核心架構改造**：
  - **權限與排班角色徹底解耦**：後端 `Code.gs` 之 `handleGetInitialData` 僅當角色確為 `Manager` 時才標記為高管自主排班。職員（Staff）與組長（Leader）即使具備後台管理權限，依然維持原出勤排班身分。
  - **落實雲端單一真理源 (Single Source of Truth)**：重構 `App.jsx` 的 `handlePullFromCloud` 機制，刷新時以雲端試算表為最高標準覆蓋記憶體，本地端僅保留加鹽雜湊（`pin_hash`, `salt`）以保障無密碼登入。
  - **舊站點代碼動態遷移容錯 (Auto-Migration on the fly)**：後端 `handleGetInitialData` 自動就地升級歷史舊代碼；前端透過 `normalizeStationId` 統一站點代碼與大小寫比對。

---

### 5. 同仁清單全域結構化三階層排序與組長選派
- **同仁名冊排序**：第一順位主屬站點（A-Z） ➜ 第二順位業務角色（高管 ➜ 組長 ➜ 正職 ➜ 計時） ➜ 第三順位員工工號。
- **組長選派**：嚴格過濾主屬或跨組支援該站點之人員，本站優先置頂，排除計時與高管，清楚標註工號與支援站點。

---

### 6. 清潔組 (ST_CLEAN) 屬性全面解禁與排班互助
- 解除原本對清潔組設定的雙向隔離限制，外組同仁可勾選支援清潔組並擔任組長協助排班；清潔組同仁亦可支援其他營業站點，演算法動態相容排班。

---

### 7. 勞基法 37/39 條國定假日調移與出勤免雙薪協議
- 新增 `holidayTransferStore.js` 與 `AnnualHolidayTransfer.jsx`。
- 全年度各月份法定放假天數動態加總平帳（非寫死 120 天，支援專案借還假調移）。
- 在同仁專屬工作台（`MyDashboard.jsx`）加裝勞基法第 37/39 條電子合意簽認卡片，完成數位簽核與稽核軌跡。

---

### 8. 全域錯誤邊界防護（ErrorBoundary）
- 在 `src/main.jsx` 外圍加裝全域 **`ErrorBoundary.jsx`（錯誤邊界保護組件）**。
- 阻絕未來任何非預期錯誤導致全頁白屏，並提供深色診斷卡片與一鍵快取清除重載按鈕，確保門市現場 100% 高可用性。

---

## 🛠️ 系統重要檔案清單與職責索引

| 檔案路徑 | 模組說明 |
| :--- | :--- |
| `src/main.jsx` | React 根入口，掛載 `ErrorBoundary` 全域錯誤防護 |
| `src/components/ErrorBoundary.jsx` | 全域錯誤攔截器，防止白屏並呈現錯誤堆疊診斷與快取重置 |
| `src/App.jsx` | 核心狀態容器，管理雲端自動拉取、單一真理源同步、15分鐘閒置監聽、排班矩陣與身分導航 |
| `src/utils/sessionUtils.js` | 會話管理工具，負責公用電腦 `sessionStorage` 與個人裝置 72 小時過期校驗 |
| `src/components/Auth/LoginView.jsx` | 登入模組，整合工號/PIN碼驗證、無痕五步暗號手勢與二階 ADMIN 控制台 |
| `src/components/Header.jsx` | 頂部導航，包含雲端同步指示、月份工時切換與手動刷新試算表按鈕 |
| `src/components/Admin/PersonnelManagement.jsx` | 同仁資料維護、三階層排序、排班組長動態選派、跨組支援清單（清潔組已解禁） |
| `src/engine/schedulerEngine.js` | 核心排班啟發式貪婪演算法（支援站點動態相容性、清潔組互助排班調度） |
| `src/components/ScheduleTable.jsx` | 全館出勤大表，內建組別/角色篩選、一鍵智慧排班與雲端儲存 |
| `src/components/Dashboard/MyDashboard.jsx` | 同仁個人工作台，整合假別明細、國假調移同意簽署 (37/39條) 與自調挪休 |
| `src/data/holidayTransferStore.js` | 國定假日調移與年度平帳資料核心，支援個別同意檢查 |
| `src/data/swapStore.js` | 調班申請、合規預檢與不可抹滅雙快照審計日誌 |
| `src/services/apiService.js` | 雲端 JSON-RPC 網關，統籌 Google Apps Script 雙向讀寫 |
| `src/backend/Code.gs` | Google Apps Script 原生後端（權限解耦、資料庫欄位動態遷移、加鹽雜湊、班表儲存） |
| `.env.local` | 本機專用機密檔（未追蹤），包含 GAS API 端點、金鑰與管理員手勢密碼說明 |
| `vercel.json` | Vercel SPA 路由重導向配置檔 |
| `package.json` | 專案依賴與建置設定（`build: "vite build"`） |

---

## 🚀 日常維護與後續交接指引

### 1. 程式碼發布與部署流程
本專案採用 **Vercel + GitHub CI/CD** 全自動化發布架構：
```bash
cd c:\Github\ReactApp\xuelu-shift-frontend
git add .
git commit -m "feat: 說明本次更新內容"
git push origin main
```
- 推送至 `main` 分支後，Vercel 會在 30 秒內自動拉取最新程式碼完成打包並無縫發布。
- 現場同仁使用手機或電腦瀏覽器重新整理（或重開 PWA），即可立即使用最新功能。

### 2. Google Apps Script (GAS) 後端部署提醒
若有異動 `src/backend/Code.gs`：
- 本專案已配置 clasp 自動化發布流水線，請在終端機直接執行：
  ```bash
  npm run gas:deploy
  ```
  系統會自動執行代碼推送與線上部署版本更新，**絕對無須手動複製貼上**。

### 3. Google 試算表與線上系統資料同步
- **名冊/站點/規則更新**：任何人在 Google 試算表修改資料後，線上系統使用者僅需點擊頂部導航列的 **「🔄 刷新試算表」**，系統將以試算表為唯一真理源，即刻重載最新資料。
- **班表產出與永久儲存**：主管在「排班總表」點擊 **「🚀 啟動智慧排班」** 生成最佳班表後，點擊 **「☁️ 儲存至 Google 試算表」**，即會全量寫入試算表的 `Schedules` 工作表持久化。
